<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- Phone / Calls -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>

    <!-- Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Vibrate -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Device Admin (for power/reboot) -->
    <uses-permission android:name="android.permission.REBOOT"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAXI"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Settings -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"/>

        <!-- Security Activities -->
        <activity
            android:name=".security.AppLockActivity"
            android:exported="false"
            android:theme="@style/Theme.MAXI"/>
        <activity
            android:name=".security.AppSelectionActivity"
            android:exported="false"/>
        <activity
            android:name=".security.PatternSetupActivity"
            android:exported="false"/>
        <activity
            android:name=".security.SecuritySettingsActivity"
            android:exported="false"/>

        <!-- Call Assistant -->
        <activity
            android:name=".ui.main.CallAssistantActivity"
            android:exported="false"/>

        <!-- Accessibility Service -->
        <service
            android:name=".service.AccessibilityHelperService"
            android:exported="true"
            android:label="MAXI Assistant"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- Smart Accessibility Engine -->
        <service
            android:name=".service.SmartAccessibilityEngine"
            android:exported="true"
            android:label="MAXI Smart Engine"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- Overlay / Bubble Service -->
        <service
            android:name=".overlay.FloatingBubbleService"
            android:exported="false"
            android:foregroundServiceType="microphone"/>

        <!-- Overlay Service (Myra) -->
        <service
            android:name=".service.MyraOverlayService"
            android:exported="false"
            android:foregroundServiceType="microphone"/>

        <!-- Main Assistant Services -->
        <service
            android:name=".services.MainAssistantService"
            android:exported="false"
            android:foregroundServiceType="microphone"/>

        <service
            android:name=".services.ForegroundVoiceService"
            android:exported="false"
            android:foregroundServiceType="microphone"/>

        <service
            android:name=".services.RealtimeConversationService"
            android:exported="false"
            android:foregroundServiceType="microphone"/>

        <!-- Call Monitor -->
        <service
            android:name=".service.CallMonitorService"
            android:exported="false"/>

        <!-- Boot Receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Power Button Receiver -->
        <receiver
            android:name=".service.PowerButtonReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON"/>
                <action android:name="android.intent.action.SCREEN_OFF"/>
            </intent-filter>
        </receiver>

        <!-- Device Admin -->
        <receiver
            android:name=".service.MyraDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver"/>
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
